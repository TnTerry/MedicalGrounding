# Medical Grounding

## Environment Setup
1. Clone the repo
```
git clone -b terry-branch https://github.com/TnTerry/MedicalGrounding.git
cd MedicalGrounding
```
2. Create an environment and install the packages
```
conda create -n medgrounding python=3.10 -y
pip install -r requirements.txt
```
3. 
